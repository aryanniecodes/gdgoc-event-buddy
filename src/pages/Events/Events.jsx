import PageShell from '../../components/common/PageShell'

function Events() {
  return (
    <PageShell
      description="Event listings, filters, and registration flows will be added after the foundation."
      eyebrow="Community"
      title="Events"
    />
  )
}

export default Events
